
#### Setup
- Installing the project dependencies
  > Run the command below
  ```shell
  $ yarn install
  ```
- Start 
  > run the command below
  ```shell
  $ yarn dev
  ```
- Use `http://localhost:3000`

